export enum AuthConstants {
    USER_NOT_FOUND = "User Not Found",
    WRONG_PASSWORD = "Credentials Invalid !!",
    UserExists = 'User Already Exists',
    InvalidEmail = 'Invalid Email',
    WrongOTP = 'Wrong OTP',
    OTPExpired = 'OTP Expired',
    OTPVerified = 'OTP Verified'
}
